{
  "name": "@belyas/pubsub-mfe-docs",
  "version": "1.0.0",
  "description": "Documentation site for @belyas/pubsub-mfe",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "pnpm run build && gh-pages -d .vitepress/dist"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.13",
    "gh-pages": "^6.2.0"
  }
}
